# ROS 2 Minimal Subscriber Example

This is a simple subscriber example that demonstrates the basic structure of a ROS 2 subscriber node.

```python
import rclpy
from rclpy.node import Node
from std_msgs.msg import String


class MinimalSubscriber(Node):

    def __init__(self):
        super().__init__('minimal_subscriber')
        self.subscription = self.create_subscription(
            String,
            'topic',
            self.listener_callback,
            10)
        self.subscription  # prevent unused variable warning

    def listener_callback(self, msg):
        self.get_logger().info('I heard: "%s"' % msg.data)


def main(args=None):
    rclpy.init(args=args)

    minimal_subscriber = MinimalSubscriber()

    rclpy.spin(minimal_subscriber)

    # Destroy the node explicitly
    minimal_subscriber.destroy_node()
    rclpy.shutdown()


if __name__ == '__main__':
    main()
```

## Explanation:

1. Import necessary modules
2. Create a class that inherits from Node
3. Initialize subscriber in constructor with callback function
4. Define callback function that processes received messages
5. Initialize and run the node in main function